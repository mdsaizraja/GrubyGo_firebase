import { Platform } from "react-native";

// This is the public URL for your backend service running in the cloud IDE.
const HOSTNAME = "4040-firebase-grubygo-1756924911555.cluster-ancjwrkgr5dvux4qug5rbzyc2y.cloudworkstations.dev";

// It's common for these preview URLs to use HTTPS.
export const BASE_URL = `https://${HOSTNAME}/api`;
export const SOCKET_URL = `https://${HOSTNAME}`;

export const GOOGLE_MAP_API_KEY = "YOUR_GOOGLE_MAP_API_KEY";
